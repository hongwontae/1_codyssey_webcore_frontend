1. 배열 구조 분해
const numbers = [10, 20, 30];

const [a, b, c] = numbers;


2. 객체 구조 분해
const numbers = [10, 20, 30];

const [a, b, c] = numbers;

const { name, age } = user;


3. 화살표 함수
    => 함수를 짧게 표현하는 방식


4. 비동기 함수
    => 비동기 함수가 promise를 반환하면 then을 통해 promise가 fulfill 상태일 떄 실행할 코드를 등록할 수 있습니다.
    => 이 떄 이 외부문은 실행되고 있습니다.

    => 비동기 함수가 async 함수 내부에 존재하면 await를 만나면 비동기 처리 전까지 async가 기다립니다.
    => 이 떄 외부문은 실행되고 있습니다.


5. let과 const의 차이는 재할당


6. 모바일 퍼스트
    => 모바일은 공간이 적어서 정말 필요한 콘텐츠와 기능이 무엇인지 결정할 수 있다.
    => 
